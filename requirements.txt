fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # For file uploads / form data

# Database (choose one or both)
sqlalchemy>=2.0.0
alembic>=1.13.0                  # Database migrations
psycopg2-binary>=2.9.0           # PostgreSQL driver
# pydantic-settings>=2.0.0       # Newer way to handle settings (Pydantic v2)

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart

# CORS & Utils
fastapi-cors>=0.0.1              # or just use FastAPI's built-in
python-dotenv>=1.0.0

# Location / Geospatial (very useful for "Work Near")
geopy>=2.4.0                     # Geocoding & distance calculations
geographiclib>=2.0

# Optional but recommended
httpx>=0.27.0                    # For making HTTP requests
pydantic>=2.8.0
email-validator>=2.2.0           # For email validation
redis>=5.0.0                     # If you need caching / rate limiting / sessions